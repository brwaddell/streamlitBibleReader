# Copy to secrets.toml for local dev, or add these in Streamlit Community Cloud dashboard.
# Never commit secrets.toml (it's in .gitignore).

SUPABASE_URL = "https://your-project.supabase.co"
SUPABASE_ANON_KEY = "your-anon-key"       # For auth (login)
SUPABASE_SERVICE_KEY = "your-service-key" # For storage + database
OPENAI_API_KEY = "sk-..."
GEMINI_API_KEY = "..."
